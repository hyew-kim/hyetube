extends base
block content
    h2=pageTitle
    em=errorMessage
    form(method="POST")
        label(for="oldPassword") Current password
        input(id="oldPassword", name="oldPassword", type="password",placeholder="Old Password", required)
        label(for="newPassword") New password
        input(id="newPassword", name="newPassword", type="password",placeholder="New Password", required)
        label(for="newPassword2") Confrim New password
        input(id="newPassword2", name="newPassword2", type="password",placeholder="New Password Confirm", required)
        input(type="submit", value="Done")