extends base
include mixins/video

block content
    div#container.home
        h2.ir-hidden Search
        each video in videos
            +video(video)
        else
            em There's no content.